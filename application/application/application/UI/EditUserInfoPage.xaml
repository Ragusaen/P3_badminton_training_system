<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="application.UI.EditUserInfoPage">

    <NavigationPage.TitleView>
        <StackLayout Orientation="Horizontal" Spacing="10" x:Name="Navigationbar">
            <Label Text="Edit User's information" FontAttributes="Bold" FontSize="30" TextColor="White" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>
            <ImageButton Source="" x:Name="Plusicon" HeightRequest="25" VerticalOptions="CenterAndExpand" HorizontalOptions="End" Margin="0,0,15,0" Command="{Binding AddClickCommand}" BackgroundColor="Red"/>
        </StackLayout>
    </NavigationPage.TitleView>

    <StackLayout>
            <Label Text="Full Name"/>
            <Entry Text="{Binding FullName}"/>
            <Label Text="Username"/>
            <Entry Text="{Binding Username}"/>
            <Label Text="Password"/>
            <Entry Text="{Binding Password}"/>

        </StackLayout>
</ContentPage>