<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="application.UI.PlaySessionPage">
    <ContentPage.Content>
        <StackLayout>
            <Label x:Name="Name" Text="name" FontSize="Large" HorizontalOptions="Center"/>
            <Label x:Name="Date" Text="date" />
            <Label x:Name="Time" Text="time" />
            <Label x:Name="Location" Text="{Binding PlaySession.Location}"/>

            <StackLayout x:Name="PracticeRelevant">
                <Frame CornerRadius="4" HasShadow="True" Margin="15,0,15,0">
                    <StackLayout>
                        <StackLayout Orientation="Horizontal" HorizontalOptions="Center" VerticalOptions="Center">
                            <Image Source="" x:Name="BullsEyeIcon" HeightRequest="40"/>
                            <Label Text="Focus Points" FontSize="Title" VerticalOptions="Center"/>
                        </StackLayout>
                        <Label Text="Select Main Focus Point" FontSize="20" HorizontalOptions="Start" VerticalOptions="Center"/>
                        <ListView VerticalScrollBarVisibility="Never" ItemsSource="{Binding FocusPoints}" VerticalOptions="Fill" SeparatorVisibility="None" HeightRequest="{Binding FocusPointListHeight}" SelectedItem="{Binding Practice.MainFocusPoint}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <ViewCell>
                                        <Label Grid.Column="0" Text="{Binding Descriptor.Name}" VerticalOptions="Center"/>
                                    </ViewCell>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackLayout>
                </Frame>

                <ListView x:Name="Exercises" ItemsSource="{Binding Exercises}" RowHeight="235" Margin="15,0,15,0" HeightRequest="{Binding PlanHeight}" VerticalScrollBarVisibility="Never" SeparatorVisibility="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Frame CornerRadius="5" HasShadow="True" Margin="0,10,0,10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="1.5*"/>
                                            <RowDefinition Height="1*"/>
                                            <RowDefinition Height="3*"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1.5*"/>
                                            <ColumnDefinition Width="4*"/>
                                            <ColumnDefinition Width="1*"/>
                                        </Grid.ColumnDefinitions>
                                        <Label Grid.Row="0" Grid.Column="0" Text="{Binding Minutes}" HorizontalOptions="Start" HorizontalTextAlignment="End"/>
                                        <Label Grid.Row="0" Grid.Column="1" Text="{Binding ExerciseDescriptor.Name}"  VerticalOptions="Center" FontSize="18"/>
                                        <Label Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Text="Exercises" FontSize="20" TextDecorations="Underline" HorizontalOptions="Center"/>
                                        <Label Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" FontSize="18" Text="{Binding ExerciseDescriptor.Description}" VerticalOptions="Fill"/>
                                    </Grid>
                                </Frame>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Button Text="Submit feedback for this practice" Command="{Binding FeedbackCommand}" IsVisible="{Binding PracticeFeedbackIsVisable}"/>
            </StackLayout>

            <StackLayout x:Name="TeamMatchRelevant">

            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>